import React from "react";

export const Images = () => {
  let images = [
    LionRed,
    LionOrange,
    LionYellow,
    LionGreen,
    LionTeal,
    LionBlue,
    LionPurple,
    LionPink,
  ];

  return <div>{images}</div>;
};
